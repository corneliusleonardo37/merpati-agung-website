---
import { whatsappUrl } from '../data/site';

const currentPath = Astro.url.pathname;

const navItems = [
  {
    label: 'Layanan',
    href: '/layanan',
    isActive: currentPath === '/layanan',
  },
  {
    label: 'Sistem Verd',
    href: '/sistem-verd',
    isActive: currentPath === '/sistem-verd',
  },
  {
    label: 'Portfolio',
    href: '/portfolio',
    isActive: currentPath === '/portfolio',
  },
  {
    label: 'Kontak',
    href: '/kontak',
    isActive: currentPath === '/kontak',
  },
];
---

<header class="fixed left-0 top-0 z-50 w-full border-b border-stone-200/70 bg-stone-50/90 backdrop-blur">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-3">
    <a href="/" class="flex items-center gap-3 font-bold tracking-tight text-stone-950">
      <img
        src="/verd-logo.svg"
        alt="Verd logo"
        class="h-8 w-auto"
      />
    </a>

    <nav class="hidden items-center gap-8 text-sm font-medium text-stone-700 md:flex">
      {navItems.map((item) => (
        <a
          href={item.href}
          class:list={[
            'transition hover:text-emerald-800',
            item.isActive ? 'font-bold text-emerald-800' : 'text-stone-700',
          ]}
        >
          {item.label}
        </a>
      ))}
    </nav>

    <a
      href={whatsappUrl}
      target="_blank"
      class="hidden rounded-full bg-emerald-800 px-4 py-2 text-sm font-semibold text-white transition hover:bg-emerald-900 md:inline-flex"
    >
      WhatsApp
    </a>

    <button
      id="mobile-menu-button"
      type="button"
      class="rounded-full border border-stone-300 px-4 py-2 text-sm font-semibold text-stone-800 md:hidden"
      aria-label="Buka menu navigasi"
      aria-expanded="false"
    >
      Menu
    </button>
  </div>

  <div
    id="mobile-menu"
    class="hidden border-t border-stone-200 bg-stone-50 px-6 py-4 md:hidden"
  >
    <nav class="mx-auto flex max-w-6xl flex-col gap-4 text-sm font-medium">
      {navItems.map((item) => (
        <a
          href={item.href}
          class:list={[
            'transition hover:text-emerald-800',
            item.isActive ? 'font-bold text-emerald-800' : 'text-stone-700',
          ]}
        >
          {item.label}
        </a>
      ))}

      <a
        href={whatsappUrl}
        target="_blank"
        class="mt-2 rounded-full bg-emerald-800 px-4 py-3 text-center font-semibold text-white transition hover:bg-emerald-900"
      >
        Konsultasi via WhatsApp
      </a>
    </nav>
  </div>
</header>

<script>
  const menuButton = document.querySelector('#mobile-menu-button');
  const mobileMenu = document.querySelector('#mobile-menu');

  menuButton?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');

    const isOpen = !mobileMenu?.classList.contains('hidden');
    menuButton.setAttribute('aria-expanded', String(isOpen));
    menuButton.textContent = isOpen ? 'Tutup' : 'Menu';
  });
</script>